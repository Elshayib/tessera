# Point is optional; words still work alone

The Person may click an Object to mean "this." They never have to. The core loop is still words: the Agent names Objects in Narration so "the roof" has something to bind to. Requiring a click was rejected: that is a hidden gizmo and breaks beginner-first. Words-only with no Point was rejected: "that one" would become an argument with the Agent.
