/**
 * Placeholder package name for the `@tessera/std` workspace package.
 *
 * Real primitives (Result, ids, Logger, …) land in T-0002.
 *
 * @public
 */
export const PACKAGE_NAME = "@tessera/std" as const;
