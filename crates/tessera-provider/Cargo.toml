[package]
name = "tessera-provider"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tessera-session = { path = "../tessera-session" }
serde = { workspace = true }
serde_json = { workspace = true }
ureq = { version = "3", features = ["json"] }

[dev-dependencies]
serde_json = { workspace = true }
tessera-session = { path = "../tessera-session" }
